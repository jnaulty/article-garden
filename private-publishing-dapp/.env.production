# Production Environment Configuration for article.garden
# This file is used when building for production deployment

# Sui Network Configuration
VITE_SUI_NETWORK=testnet

# Package and Object IDs for Testnet
# TODO: Update these with your actual deployed package and object IDs on testnet
# VITE_PACKAGE_ID=0x...
# VITE_PUBLISHER_OBJECT_ID=0x...
# VITE_KIOSK_OBJECT_ID=0x...

# Walrus Configuration (Testnet)
# TODO: Update with testnet Walrus endpoints if different from defaults
# VITE_WALRUS_AGGREGATOR_URL=https://aggregator-testnet.walrus.space
# VITE_WALRUS_PUBLISHER_URL=https://publisher-testnet.walrus.space

# Seal Configuration (Testnet)
# TODO: Update with testnet Seal endpoints if different from defaults
# VITE_SEAL_API_URL=https://seal-testnet.mysten.io

# Application Configuration
VITE_APP_NAME=Article Garden
VITE_APP_ENV=production

# Optional: Analytics and Monitoring
# VITE_ANALYTICS_ID=
# VITE_SENTRY_DSN=

# Notes:
# - All environment variables must be prefixed with VITE_ to be available in the app
# - Update the TODO items with your actual testnet deployment values
# - Keep this file in version control (it contains no secrets, just configuration)
# - For local development, use .env.local which is gitignored
